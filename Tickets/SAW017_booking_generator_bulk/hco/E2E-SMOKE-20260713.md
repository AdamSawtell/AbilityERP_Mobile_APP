# SAW017 — HCO E2E smoke evidence (2026-07-13)

Host: `32.236.127.117` · Role: Admin · Commit at smoke: `c44b3db`+  

**No HCO `*_UU` values changed.**

## Pass criteria

| Step | Result | Evidence |
|------|--------|----------|
| Bundles present | PASS | `com.aberp.servicebooking.generator` patched JAR + `com.aberp.bookinggenerator.bulk` in `bundles.info` |
| Cache Reset | PASS | WebUI Menu → Cache Reset |
| Single Generate Bookings | PASS | BG Search Key `2001124` / ID `1000910`, period **1–7 Sep 2026** → SB **`53324`**, InvoiceRule **`I`**, DocStatus **`DR`**, `ad_pinstance` result=1 OK |
| Bulk Generate Bookings | PASS | Menu process; Activity Short Term Accommodation; Include STR=Y; **1–7 Sep 2026**; DocAction Drafted |
| Bulk process summary | PASS | `created/ok=3, skipped=0, failed=0 (candidates=3)` · logs OK BG `1000393`, `1000509`, `1000841` |
| Bulk SBs + Invoice Rule | PASS | SB **`53325`** (BG `1000393`), **`53326`** (BG `1000509`) — both InvoiceRule **`I`**, DR, dateordered 2026-09-01 |
| Existing Generate Bookings unchanged | PASS | Still separate button/process; bulk is additive |

## Notes for future deploys

1. **DocAction** list must be **`BookingGen_DocList`** (UU `285220bc-9749-4c4b-978d-4674fad038cd`). Apply `sql/02-fix-docaction-list.sql` after install. Core `_Document Action` (135) has no `DR`.
2. After AD changes: **Cache Reset** (or re-login) before smoke.
3. Prefer a **clean future period** (e.g. next month) so new SBs are obvious.
4. For small bulk smoke: Activity **Short Term Accommodation** + Include STR = **Y** (only ~3 STANDARD STR rows on HCO Test).
5. Generate Bookings may return OK with **no new order** if patterns already produce nothing for that period; bulk still counts `ok` and still applies run dates on BG.
6. `ad_pinstance` lookups: filter by `ad_process_id` + `created` window — full-table joins time out on HCO.
7. Date From / Date To / Activity defaults must stay **empty** in production AD (operators enter per run). Do not leave smoke defaults behind.

## Process / object UUs (portable)

| Object | UU / value |
|--------|------------|
| Bulk process | `17a01701-b017-4017-8017-000000000001` / `AbERP_BG_BulkGenerateBookings` |
| Generate Bookings | `6482f6b8-eaa3-4e7b-a8f6-4e263d44909b` / `Generate Bookings` |
| BookingGen_DocList | `285220bc-9749-4c4b-978d-4674fad038cd` |
| Booking Generator window | `de336034-bd4e-4445-b018-9c762c98d847` |
